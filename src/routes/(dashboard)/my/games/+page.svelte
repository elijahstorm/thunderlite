<!-- Never rendered: the load in +page.ts always redirects to /my/history. This
     file exists because SvelteKit requires a page component alongside a page
     load. -->
<p class="text-sm text-muted-foreground">Redirecting to your match history…</p>
